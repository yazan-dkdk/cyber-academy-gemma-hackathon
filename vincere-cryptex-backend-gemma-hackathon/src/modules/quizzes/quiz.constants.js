export const QuizStatuses = Object.freeze({
  DRAFT: 'draft',
  PUBLISHED: 'published'
});

export const QuizAttemptStatuses = Object.freeze({
  IN_PROGRESS: 'in_progress',
  SUBMITTED: 'submitted'
});

export const QuizTargetTypes = Object.freeze({
  COURSE: 'course',
  LESSON: 'lesson'
});

export const QuizQuestionTypes = Object.freeze({
  MCQ: 'mcq'
});
