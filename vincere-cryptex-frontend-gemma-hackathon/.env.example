# Local development example values only. Do not commit real secrets.

# NestJS backend API origin. The frontend dev server runs on http://localhost:3001.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
